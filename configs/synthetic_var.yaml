dataset:
  kind: var
  n_nodes: 8
  timesteps: 1000
  max_lag: 3
  edge_prob: 0.18
  noise_scale: 0.10
  seed: 0

evaluation:
  scorer: lagged_correlation
  metrics:
    - edge_auc
    - precision_at_k
    - lag_accuracy

output:
  save_npz: false
  path: results/tables/synthetic_sanity.npz
